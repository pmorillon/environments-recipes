#==============================================================================
# vim: softtabstop=2 shiftwidth=2 expandtab fenc=utf-8 cc=81 tw=80
#==============================================================================
#
# DESCRIPTION: Debian 7 (Wheezy) appliance built with chroot and qemu-nbd.
#
#==============================================================================
---
extend: ../../kameleon/virtualbox/debian7.yaml
# Enables qcow2 checkpoint
checkpoint: virtualbox.yaml
#== Global variables use by Kameleon engine and the steps
global:
  bootstrap_packages: >
    linux-image-$$kernel_arch
    isc-dhcp-client ifupdown iptables iproute
    openssh-server
  setup_packages: >

bootstrap:
  - "@base"

setup:
  - configure_apt:
    - repository: $$apt_repository
    - enable_contrib_repo: $$apt_enable_contrib
    - enable_nonfree_repo: $$apt_enable_nonfree
  - upgrade_system:
    - dist_upgrade: true
  - install_software:
    - packages: $$setup_packages
  - setup_orchestrator
  - run_orchestrator

export:
  - disable_checkpoint
  - virtualbox_save_appliance:
    - output: "$$kameleon_cwd/$${kameleon_recipe_name}"
    - save_as_qcow2
    - save_as_tar_gz

